# Layer 1 — Nortek $PNOR ASCII stream parser (SeaExplorer real-time payload feed).
#
#   $PNORI — configuration (beams, cells, blanking, cell size, coord system)
#   $PNORS — per-ensemble sensors (date/time, sound speed, heading, pitch, roll, P, T)
#   $PNORC — per-cell beam velocities + amplitude + correlation
#
# Enables real-time/piloting products and cross-checks of the delayed pipeline.
# Implemented in Phase 5.
